import { Text, View } from 'react-native';
import { ProfileScreenProps } from '../utils/navigation-types';

const Profile = ({ route, navigation }: ProfileScreenProps) => {
    return (
        <View>
            <Text>Profile</Text>
        </View>
    );
}
export default Profile;