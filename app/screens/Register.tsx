import { Text, View } from 'react-native';
import { RegisterScreenProps } from '../utils/navigation-types';

const Register = ({ route, navigation }: RegisterScreenProps) => {
    return (
        <View>
            <Text>Register</Text>
        </View>
    );
}
export default Register;